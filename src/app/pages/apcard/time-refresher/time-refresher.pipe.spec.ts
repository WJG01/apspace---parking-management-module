import { TimeRefresherPipe } from './time-refresher.pipe';

describe('TimeRefresherPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeRefresherPipe();
    expect(pipe).toBeTruthy();
  });
});
