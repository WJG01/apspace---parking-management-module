import { ValidateCheckboxPipe } from './validate-checkbox.pipe';

describe('ValidateCheckboxPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidateCheckboxPipe();
    expect(pipe).toBeTruthy();
  });
});
