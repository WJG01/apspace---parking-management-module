import { MonthColorPipe } from './month-color.pipe';

describe('MonthColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthColorPipe();
    expect(pipe).toBeTruthy();
  });
});
