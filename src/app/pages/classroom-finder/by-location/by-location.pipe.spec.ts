import { ByLocationPipe } from './by-location.pipe';

describe('ByLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new ByLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
