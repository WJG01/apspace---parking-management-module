import { FormatByDayPipe } from './format-by-day.pipe';

describe('FormatByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
