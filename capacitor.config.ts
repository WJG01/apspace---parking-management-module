import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'my.edu.apiit.apspace',
  appName: 'APSpace',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
